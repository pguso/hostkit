{% extends 'HostkitShopBundle:Default:index.html.twig' %}

{% block content %}
    <div class="container padding-top">
        <!-- Example row of columns -->
        <div class="row">
            <div id="searchbox">

            </div>
            <h3>Shared Hosting Packages</h3>
            <hr />
            <table class="table table-bordered table-striped table-pricing">
                <thead>
                <tr>
                    <th></th>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <th>{{ package.name }}</th>
                        {% endfor %}
                    {% endif %}
                </tr>
                </thead>
                <tbody>
                <tr>
                    <td>Webspace</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td></td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>Traffic</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td></td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>Domains</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td></td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>Addon Domains</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td></td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>Park Domains</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td></td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>FTP Accounts</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td></td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>MySQL Databases</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td></td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>E-Mail Accounts</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td></td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>Subdomains</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td></td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>Monthly Price</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td>{{ package.price }}</td>
                        {% endfor %}
                    {% endif %}
                </tr>
                <tr>
                    <td>Setup</td>
                    {% if packages is iterable %}
                        {% for package in packages %}
                            <td><span class="fa fa-check"></span></td>
                        {% endfor %}
                    {% endif %}
                </tr>

                </tbody>
            </table>


        </div>

{% endblock %}