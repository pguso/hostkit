<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="form">
    {{ form_widget(form) }}
    <div class="action clearfix">
        <button type="submit" class="button button-gray flt_l">{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}</button>
    </div>
</form>
